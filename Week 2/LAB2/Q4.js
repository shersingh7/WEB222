let quotient;
const num1 = parseInt(prompt("Enter the first num: "));
const num2 = parseInt(prompt("Enter the second num: "));

if (num2 != 0)
{
  quotient = num1 / num2;
}

console.log(`The quotient of the two numbers is ${quotient}`);